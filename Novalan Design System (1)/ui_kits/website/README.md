# UI kit — Sitio de marca (novalan.mx)

Public marketing site: editorial hero, seasonal collection grid, fabric catalogue table, mill story, contact.

| File | Contents |
| --- | --- |
| `index.html` | Interactive shell; click the nav to move between routes |
| `Chrome.jsx` | `SiteHeader` (sticky, hairline bottom), `SiteFooter` (ink theme) |
| `Home.jsx` | 21:9 hero over a woven ground, manifesto, seasonal grid, colourway strip, wholesale band |
| `Pages.jsx` | `Colecciones` (filtered grid), `Tejidos` (spec table + sample request), `Taller` (timeline + traceability), `Contacto` |

Notes
- Every image is a `Figure` placeholder — no brand photography was supplied. Swap in `src` when it exists.
- Copy is Mexican Spanish, "nosotros" for the house and "tú" for the customer.
- Composed only from the published components; nothing is re-implemented locally.
