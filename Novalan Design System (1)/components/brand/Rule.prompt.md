Section divider. Novalan separates content with hairlines instead of cards or shadows, so this is the most-used layout component in the system.

```jsx
<Rule label="Colección" />
<Rule tone="solid" />
<Rule vertical />
```