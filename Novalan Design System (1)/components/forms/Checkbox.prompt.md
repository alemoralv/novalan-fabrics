Multi-select option, also used for filter lists in the fabric catalogue.

```jsx
<Checkbox label="Algodón orgánico" checked={on} onChange={setOn} />
<Checkbox label="Certificado GOTS" description="Solo lotes con certificado vigente" />
```