Setting that applies instantly — visibility toggles in the ops dashboard, notification preferences.

```jsx
<Switch label="Mostrar solo lotes disponibles" checked={on} onChange={setOn} />
```

For form values that need saving, use Checkbox instead.