Intentional addition — the brand has no image library yet, so every picture slot is a Figure. Drop in `src` when real photography arrives.

```jsx
<Figure ratio="4 / 5" tone="khaki" weave="canvas" label="Flat-lay lino" caption="Lino lavado, 240 g/m²" />
<Figure ratio="16 / 9" tone="ink" overlay={<h2 style={{color:"inherit"}}>Otoño 2026</h2>} />
```

Portrait 4:5 for product, 16:9 for editorial banners, 1:1 for grids.